'use strict';

var DAGLink = require('./index.js');

function create(name, size, multihash, callback) {
  var link = new DAGLink(name, size, multihash);
  callback(null, link);
}

module.exports = create;