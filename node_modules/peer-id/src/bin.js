#!/usr/local/bin/node


'use strict';

var PeerId = require('./index.js');

PeerId.create(function (err, id) {
  if (err) {
    throw err;
  }

  console.log(JSON.stringify(id.toJSON(), null, 2));
});